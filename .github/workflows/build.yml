name: Build and Release pgFlow

permissions:
  contents: write

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build pgFlow SQL
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install dependencies
        run: sudo apt-get install -y postgresql-client make

      - name: Ensure scripts are executable
        run: chmod +x *.sh

      - name: Set version
        id: version
        shell: bash
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
          VERSION="${GITHUB_REF#refs/tags/v}"
          else
          VERSION="dev-$(git rev-parse --short HEAD)"
          fi

          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "Resolved version: $VERSION"

      - name: Build artifacts
        run: make all

      - name: Find built SQL artifact and create stable copy
        id: find_sql
        run: |
          set -euo pipefail

          if [ -f dist/pgflow.sql ]; then
            FILE=dist/pgflow.sql
          else
            FILE=$(ls -t dist/*pgflow*.sql 2>/dev/null | head -n1 || true)
            if [ -z "$FILE" ]; then
              FILE=$(ls -t dist/*.sql 2>/dev/null | head -n1 || true)
            fi
          fi

          if [ -z "${FILE:-}" ]; then
            echo "No SQL artifact found in dist/ — failing the job." >&2
            exit 1
          fi

          echo "Found SQL artifact: $FILE"
          echo "sql_file=$FILE" >> "$GITHUB_OUTPUT"

          mkdir -p dist
          # Copy to a canonical filename (more portable than a symlink)
          cp -a "$FILE" dist/pgflow.sql
          echo "Copied $FILE -> dist/pgflow.sql"

      - name: Smoke test install
        env:
          PGPASSWORD: postgres
        run: |
          psql -h localhost -U postgres -d postgres -f dist/pgflow.sql
          psql -h localhost -U postgres -d postgres <<'SQL'
          SELECT flow.read_db_object('pg_catalog.pg_class');
          SELECT flow.select('relname');
          SQL

      - name: Upload full build artifact
        uses: actions/upload-artifact@v4
        with:
          name: pgflow-sql
          path: dist/pgflow.sql
          retention-days: 30

      - name: Upload runtime build artifact
        uses: actions/upload-artifact@v4
        with:
          name: pgflow-runtime-sql
          path: dist/*-runtime.sql
          retention-days: 30

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/pgflow-${{ steps.version.outputs.version }}.sql
            dist/pgflow-${{ steps.version.outputs.version }}-runtime.sql
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}